import "@testing-library/jest-dom"
import { render } from "@testing-library/react"
import UtilNav from "./UtilityNav"


describe("<UtilNav>", () => {
  it('should create tag list', () => {
    render(<UtilNav />)
  })
})
